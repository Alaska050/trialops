# Pinned to the exact versions this project was built and tested against,
# so a fresh clone (and Streamlit Community Cloud) installs the same stack.
pandas==3.0.6
numpy==2.4.6
scikit-learn==1.9.1
plotly==7.1.0
streamlit==1.64.0
pytest==9.1.1
